Texture2D tex : register(t0);
SamplerState samplerLinear : register(s0);

struct PS_IN {
    float4 Pos : SV_POSITION;
    float2 UV  : TEXCOORD;
};

float4 PS(PS_IN input) : SV_Target0
{
    return tex.Sample(samplerLinear, input.UV);
}

